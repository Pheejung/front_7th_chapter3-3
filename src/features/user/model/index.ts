// Model index for the `user` slice.
// Export user-specific model helpers here, for example:
// export { default as enrichUserProfile } from './enrichUserProfile'

// Currently no helpers implemented. Add named exports here as needed.

export default {}
